[project]
name = "glo_build"
version = "0.1.0"
requires-python = ">=3.14"
description = "Lightweight build system for monorepos"
dependencies = []

[project.scripts]
glo-build = "glo_build.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "licensecheck>=2025.1.0",
    "mypy>=1.19.1",
    "pytest>=9.0.2",
    "ruff>=0.14.9",
]
